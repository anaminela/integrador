# =============================================================
# VARIÁVEIS DE AMBIENTE — G2 Arena Beach (frontend)
# -------------------------------------------------------------
# Copie este arquivo para ".env" e ajuste os valores conforme
# o seu ambiente. No Vite, TODA variável exposta ao frontend
# PRECISA começar com o prefixo "VITE_".
# =============================================================

# URL base da API do backend (Node.js + Express).
# Backend roda por padrão na porta 3000 sob o prefixo /api/v1.
VITE_API_URL=http://localhost:3000/api/v1
